# TOTEM (ZMK) — config converted from the QMK "pre-totem" rules.mk
#
# NKRO  (QMK: NKRO_ENABLE = yes)
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=y
CONFIG_ZMK_USB_LOGGING=n
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_COMBO_HOLD_TAP_OVERLAP=y
# Notes on the rest of your QMK rules.mk:
#   EXTRAKEY_ENABLE  -> media / consumer keys work by default in ZMK (no flag)
#   OLED_ENABLE      -> the wireless TOTEM has no screen; dropped
#   WPM_ENABLE       -> no ZMK equivalent on this board; dropped
#   VIA_ENABLE       -> not a ZMK concept (ZMK Studio is the analog; see notes)
#   MOUSEKEY/CONSOLE/COMMAND/BACKLIGHT/RGBLIGHT/AUDIO/LTO -> N/A or default
